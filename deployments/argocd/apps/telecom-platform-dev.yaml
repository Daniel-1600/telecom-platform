apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: telecom-platform-dev
  namespace: argocd
  labels:
    app.kubernetes.io/name: telecom-platform-dev
    app.kubernetes.io/component: platform
    app.kubernetes.io/environment: dev
    app.kubernetes.io/part-of: telecom-platform
spec:
  project: default
  source:
    repoURL: https://github.com/nutcas3/telecom-platform.git
    targetRevision: main
    path: deployments/environments/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: telecom-platform-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
